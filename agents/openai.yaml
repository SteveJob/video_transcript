interface:
  display_name: "Video Transcript"
  short_description: "Transcribe first, then let the current LLM read transcript and write podcast script"
  default_prompt: "Transcribe the video/audio into text, read the transcript in this chat context, generate a podcast script by model understanding/summarization (no script-side API call), then produce podcast audio from that script."
